@model IEnumerable<Employement_Project_MVC.Models.Job_Detail>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Job_role)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Job_type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Job_salary)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Job_description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Employer_Detail)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Job_role)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Job_type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Job_salary)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Job_description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Employer_Detail.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
